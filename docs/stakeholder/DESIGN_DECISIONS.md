# Design decisions

Current state of every design choice — one row each. Implementation specifics (crates, code layout, label syntax) live in [`docs/engineering/`](../engineering/) and the spec at [`docs/superpowers/specs/`](../superpowers/specs/).

Companion: [`ENGINEERING_RULES.md`](./ENGINEERING_RULES.md), [`PROJECT_SCOPE.md`](./PROJECT_SCOPE.md), [`PRODUCTION_DISCUSSIONS.md`](./PRODUCTION_DISCUSSIONS.md).

This repo is a reference implementation of the **microservice-template + service-catalogue pattern** — the design pattern that becomes [LocalMesh](../../README.md) once an org's SRE-owned plugins are in place. The Rust + Next.js parts are one team's microservice; observability, logging, `mesh` (envoy fleet — ingress + per-workload sidecar + egress), `auth` (dex IdP), and the optional database are catalogue modules the team includes from a stub catalogue.

## Starting assumptions

Every decision below sits on top of these. They are not design choices — they are the premises the pattern only works under.

- **Mandatory tools have to be easier to use than to work around.** If using the supported path is harder than disabling, bypassing, or stubbing it, the supported path loses. Applies universally — to SRE plugins, test frameworks, linters, security controls, anything anyone is told they have to use. Adoption depends on the easier path being the supported one.
- **Compose is the limit of what a non-ops team handles on their own.** Helm is too much; bare k8s manifests are too much. Compose is the dev-facing interface every team already touches; the prescriptive middle layer (the chart generator) shifts that workload off per-team toil and onto larger infra and catalogue investments.
- **Production-quality on dev becomes production.** Each catalogue module ships the same shape ops would run in prod, just locally-hosted. Dev gets the *real* observability stack, the *real* logging contract, the *real* identity tuple — the prod overlay swaps storage backends and the rest is the same wire. Drift between dev and prod is the single most expensive failure mode of any platform pattern; this is the structural fix.
- **One human-scale file holds the cross-stakeholder truth.** [`project.toml`](../../project.toml) is where team work, SRE oversight, and business requirements intersect — identity, compliance flags, data residency, billing, ownership — auditable at whatever level the context demands. Compliance, legal, billing, and ops all read the same file; engineering decisions don't get translated into business language.
- **The compose→helm boundary is strict on purpose.** Teams own everything inside their compose; SRE owns everything past the helm chart. The framework is opinionated on the translation in between and hands-off about what SRE puts on either side.

## Settled

**Prod-considerations** is row-specific only. The full production assumption set is in [`PROJECT_SCOPE.md`](./PROJECT_SCOPE.md).

| Area | Dev choice | Rationale | Prod-considerations |
|---|---|---|---|
| Vision | One team's microservice + a stub catalogue module. | Demonstrates the org-scale pattern: platform owns catalogue, product owns compose. | Catalogue becomes a real platform product. |
| Orchestration | Compose is source of truth; pre-commit hook generates the Helm chart. | Compose is the practical ceiling for self-service teams; chart is build output. | Chart generation runs in CI; published to an internal registry. |
| Module composition | Plugin selection lives in `project.toml`'s `plugins = [...]` array; the localmesh CLI's `build` verb renders each selected plugin's compose file (`docker-compose.yaml.gotmpl` sprig-templated, or plain `.yml` passthrough) and merges them into `localmesh/bundled.compose.yaml`, which the root `docker-compose.yml` `include:`s. Required: `observability/`, `logging/`, `mesh/` (envoy ingress + per-workload sidecar + egress), `auth/` (dex IdP); optional: `database/`. Aggregator stubs (`required.compose.yaml`, `optional.compose.yaml`) retired. Every plugin ships a `plugin.toml` manifest (identity + service ports) and a `README.md` per `docs/engineering/rules/plugin-conventions.md` §2. | Required and optional modules added by reference, not copy-paste. The `mesh` plugin is required because every cross-service hop rides envoy mTLS and browser HTTPS terminates at the ingress role; removing the include leaves the stack with no browser TLS and no east-west encryption. Templating is opt-in per plugin — only adopt `.gotmpl` when there's repetition worth collapsing. | Include path swaps to a platform-published location. The dev envoy fleet swaps for the prod mesh runtime (Istio / Linkerd / Cilium) via katenary chart selection. |
| Observability module | Off-the-shelf collector + exporters + TSDB (Time-Series Database) + dashboards. The prometheus receiver uses `docker_sd_configs` (Unix socket mount, RO) for emitter discovery. Static scrape lists retired. Host-facing scrapers (cadvisor, node-exporter) self-describe via `prometheus.io/*` labels. | All battle-tested; team adds no value building these. | TSDB swaps to a managed/clustered equivalent at scale. Host-facing scrapers (cadvisor, node-exporter, filelog) are dev-fidelity only; in prod ops owns the equivalent collectors and the team's catalog excludes them via the prod overlay. `docker_sd_configs` swaps for `kubernetes_sd_configs` in prod (pod labels carry the same `prometheus.io/*` convention). |
| OTel semconv as the omakase contract | Apps follow OpenTelemetry semantic conventions end-to-end: standard env vars (`OTEL_SERVICE_NAME`, `OTEL_RESOURCE_ATTRIBUTES`, `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`, `OTEL_SEMCONV_STABILITY_OPT_IN=http` for Node); standard metric names (`http.server.request.duration`, `rpc.server.duration`); standard resource attrs promoted to series labels (`service_name`, `service_namespace`, `service_instance_id`, `deployment_environment_name`). Single bootstrap per language: Rust extends `server/src/telemetry.rs` with a `MeterProvider` + `RpcMetricsLayer` tower middleware; Node extends `www/instrumentation.ts` with `HttpInstrumentation` + a `PeriodicExportingMetricReader` via `@vercel/otel`. Collector translates OTel→Prometheus on VM's behalf (`prometheusremotewrite` exporter with `resource_to_telemetry_conversion`); VM has an OTLP receiver but lacks `keep_identifying_resource_attributes`, which the imported dashboard's header stat panels need — the collector path supplies it. Long form: [`docs/superpowers/specs/2026-05-16-otel-semconv-pipeline-design.md`](../superpowers/specs/2026-05-16-otel-semconv-pipeline-design.md). | One env-var convention + one bootstrap file per language gives any new service the canonical OTel dashboard for free. Backend swaps (Mimir / Grafana Cloud / Prometheus 3.x with OTLP receiver) need only the resource-attr promotion knob flipped — apps and dashboards portable. | Add OTLP semconv metrics for outbound HTTP / DB / RPC clients (currently only the Node SDK auto-emits these); add `service.version` from build metadata. |
| Dashboards | Module-as-contributor pattern. Every module that ships a dashboard drops `<slug>.json` next to its `docker-compose.yml`, declares it via a top-level `configs:` entry keyed `<module>_dashboard_<slug>`, and mounts it onto the `grafana` service at `/etc/grafana/dashboards/<module>/<slug>.json`. Compose's `include:` merge unions `services.grafana.configs:` across all included modules so the destination Grafana sees every contribution automatically — no central registry, no coordination. Grafana's file provider runs with `foldersFromFilesStructure: true` so each `<module>/` subdir becomes a UI folder; `allowUiUpdates: true` for now so devs can iterate in the UI without friction. Observability is just another module that happens to host the Grafana runtime — its dashboards (`observability/apm.json`, `observability/cluster-health.json`) use the same contribution pattern. The runtime (image, datasources, provisioning config, bootstrap script) stays in observability's compose; no other module touches it. **`apm`** — imported community dashboard "[Lightweight APM for OpenTelemetry](https://grafana.com/grafana/dashboards/22784)" (Grafana ID 22784, [source](https://github.com/cyrille-leclerc/opentelemetry-service-dashboard)); patched: datasource defaults bound to our UIDs, template-variable queries source from `label_values(LABEL)` directly (VM doesn't have `keep_identifying_resource_attributes`), stat-panel filters use `job=~"$service_namespace/$service_name"`. **`cluster-health`** — host/container resources (cadvisor + node-exporter) plus a module-roster panel. **`postgres`** (database module, worked example) — community dashboard [9628](https://grafana.com/grafana/dashboards/9628) by Lucas Estienne, patched for our datasource UIDs + label conventions; adds one custom top-N slow-query table panel sourced from `pg_stat_statements`. Database's compose also ships a `postgres-exporter` sidecar (emits metrics) and a `database-collector` sidecar (scrapes the exporter, pushes OTLP to the main otel-collector). **`ingress`** (mesh module, deferred) — request rates by HTTP status, p50/p95 ingress latency, upstream health. The pre-envoy version sourced from Caddy access logs via Loki; envoy emits the same surface natively via `:15090/stats/prometheus` plus JSON access logs. The rewrite is deferred — the imported APM dashboard already covers cross-service RPC RED panels via `tracing.http`, so the standalone ingress dashboard isn't on the critical path until egress traffic patterns warrant it. `TODO: needs_prod_decisions ingress dashboard rewrite against envoy metrics`. Long form: [`docs/superpowers/specs/2026-05-17-module-dashboards-design.md`](../superpowers/specs/2026-05-17-module-dashboards-design.md). | Destination Grafana auto-discovers contributions the moment a module is `include:`d. Modules stay self-contained and removable. Importing community dashboards validates the omakase contract — if they work against this stack, they work against any OTel-conforming stack. | Harden to `allowUiUpdates: false` once the pattern is stable. Extend the same pattern to alert/recording rules (`<module>.alerts.yaml` mounted under `/etc/grafana/alerting/<module>/`). When a metrics module needs cross-module scrape contribution to the main collector, use a sidecar collector pattern (spec §6.3 — chosen here because `otel-collector-contrib:0.152.0` doesn't ship the `glob:` config provider the multi-config-merge alternative would need). |
| Module observability conventions | Compose config key naming: `<module>_dashboard_<slug>` for dashboards; `<module>_<purpose>` for other configs (e.g., `database_collector_config`, `database_pgexporter_queries`). Grafana mount target: `/etc/grafana/dashboards/<module>/<slug>.json` — the intermediate `<module>/` subdir is what triggers `foldersFromFilesStructure` to create a UI folder. Two metric-contribution patterns. Pattern B (sidecar push) — for modules that emit metrics: ship a per-module sidecar collector that scrapes the module's emitters and pushes OTLP to the main collector at `otel-collector:4317`. Stamps the standard identity tuple via the OTel resource processor (`service.name`, `module_name`, `owned_by`) — convention now canonical in [`plugin-conventions.md`](../engineering/rules/plugin-conventions.md). Pattern A (centrally scraped emitters) is label-based discovery. The main collector's prometheus receiver uses `docker_sd_configs` with relabel rules keyed on `prometheus.io/scrape=true` + `prometheus.io/port` (+ optional `prometheus.io/path`) container labels. Cross-plugin edits to observability's collector config are no longer needed for new Pattern-A targets. Pattern B stays for plugins with rich scrape config the labels can't express (e.g., postgres-exporter's custom queries file). Zero cross-module config coordination — each module is self-contained. | Pattern is uniform across modules; observability owns the runtime, modules own their data. | Revisit the sidecar-vs-overlay decision if a future collector image ships the `glob:` config provider — then the single-collector-with-overlay approach (spec §6.3) becomes viable and might be preferred for runtime efficiency. |
| Module attribution | Every application signal carries the OTel-canonical identity tuple — `service.name`, `service.namespace`, `service.instance.id`, `deployment.environment.name` — plus the two project-specific attrs `module_name` and `owned_by`. For traces and metrics, identity flows through `OTEL_SERVICE_NAME` + `OTEL_RESOURCE_ATTRIBUTES` env vars consumed by the OTel SDK at startup. The Rust SDK reads both; `@vercel/otel` (www) does the same; pg_tracing's hardcoded `service.name=PostgreSQL_Server` gets rewritten by the collector's `transform/database`. For logs, identity is supplied by Vector at ingest from the per-service `metrics.service_name` / `metrics.module_name` / `metrics.owned_by` docker labels — apps emit none of these in their stdout. Resource attrs become labels on every series (collector's `resource_to_telemetry_conversion`) and indexed Loki labels (config in `localmesh/service_catalog/logging/`). Cadvisor's `metrics.service_name` docker label still becomes a `service` (lowercase) metric label — kept as a *container-resource* lens distinct from the application-identity tuple; the two coexist intentionally. `service.namespace` tracks `${PROJECT_NAME}` (from `.env`, mirroring `project.toml`) so compose `name:` and OTel namespace share one source. The identity-tuple binding (`module_name=app` vs. `<plugin-slug>`) is canonical in [`plugin-conventions.md`](../engineering/rules/plugin-conventions.md). Two channels for the same identity (compose env for SDK, docker labels for Vector) is a known duplication — single-source consolidation is a follow-up (see Open). | One identity tuple makes the roster, RED, and owner-attribution queryable across all three signals. The OTel-canonical attrs are what the imported APM dashboard (and every off-the-shelf OTel dashboard) reads. | `owned_by` resolves from the org's team registry rather than hardcoded compose values; `service.namespace` becomes the team/tenant axis. |
| Database module (optional) | Postgres 16 + Datadog's `pg_tracing` extension, custom-built image, OTLP/HTTP push to the collector. Hard-coded sampling at 1.0; `service.name` rewritten `PostgreSQL_Server` → `postgres` at the collector. Server holds a `sqlx` `PgPool` plumbed via `DATABASE_URL`; `sqlx::migrate!()` applies embedded SQL migrations at server startup. Demo `Greeter.PrintPostgresStats` RPC writes one `noise_events` row per click and reads the five headline `pg_stat_database` metrics; each SQL statement carries a `/*traceparent='...'*/` comment so pg_tracing stitches the resulting parse/plan/exec spans under the gRPC server span. Real product use-cases (registry / audit log / events) deferred — they'll define their own schemas alongside `noise_events`. Rollbacks deliberately out of scope: "rollback" means different things across prod targets (managed-DB point-in-time-restore vs. compensating migrations vs. blue/green schema flips); `sqlx::migrate!()` doesn't preclude any of them. | Single image, zero polling glue — pg_tracing emits parse/plan/exec spans as children of any traceparent embedded as a SQL comment, so the trace `user → www → server → postgres` works end-to-end without per-team integration code. | Managed offering (RDS / CloudSQL) with pg_tracing enabled via parameter group; sampling tuned to ~1-5%. Migration-rollback strategy chosen alongside the prod DB target. |
| Server | Rust gRPC. Greeter.SayHello (trace-stitching reference) + Catalog (List/Get for datasources + metrics; full CRUD for dashboards, single-panel single-query model). tonic plaintext bound to 127.0.0.1; the `server-mesh` envoy sidecar shares the netns and terminates inbound mTLS on :50051, redirected via iptables to envoy's :15006. Traces (application metrics pending re-design); reqwest→Grafana hop is not auto-instrumented, so the Grafana HTTP leg shows as opaque inside the server span. | Preserves the original template wire-shape; trace instrumentation visible end-to-end. App code is mTLS-free — sidecar owns the termination. | Demo endpoints replaced by real product methods; metric surface added once chosen. |
| Server → metrics DB | Native postgres mTLS. `postgresql.conf` reads `id.postgres.{crt,key}` + `trust.ca.crt` from the per-plugin cert mount; `pg_hba.conf` enforces `hostssl … cert clientcert=verify-full` with the role `server` cert-CN-bound. Rust client (sqlx + tls-rustls) reads `id.server.{crt,key}` for outbound. No password fallback. `postgres-exporter` joins the mesh too with its own cert + role. Postgres and exporter both wrap their image entrypoints to copy bind-mounted certs into a postgres-owned location with `0600` .key (`pq` and postgres itself enforce strict perms). | Worked example of the inter-service mTLS pattern landed end-to-end: cert + chain + CN-to-role binding all real. | File-mounted certs replaced by SPIRE-issued short-lived SVIDs delivered via cert-manager / Workload API; pg-side strict-perm gymnastics goes away. |
| www | Next.js front-end, traces enabled, dashboard panels embedded inline. **Identity-free** — auth gating at the ingress envoy (`oauth2` filter + `jwt_authn` against dex JWKS); claim-to-header lands `x-user-{id,email,name}` on every gated request. The JWT is forwarded opaquely as the `Authorization` gRPC metadata header to server, which validates it against dex's JWKS. No cookie parsing, no `X-Forwarded-*` resolution, no env fallback in www. www's API routes use one `forwardAuth(req)` helper that copies the inbound `Authorization` header into gRPC metadata. | Pages, API routes, middleware all emit traces; Grafana owns dashboard chrome. | Iframe embeds wrapped by an auth gateway. |
| gRPC load balancing | Next.js backend dials `server:50051` plaintext; iptables in the netns redirects the SYN to the `www-mesh` envoy sidecar's outbound listener (:15001); envoy reads `SO_ORIGINAL_DST`, picks the `server-mesh` cluster off the destination port, and originates mTLS to `server-mesh:50051`, where the `server-mesh` sidecar's inbound listener terminates it (also iptables-redirected to envoy :15006) and forwards plaintext to tonic on `127.0.0.1:50052`. Wire shape is HTTP/2 gRPC over mTLS between hosts; the per-language SDK stays plaintext-only. Browser HTTPS terminates at the `ingress-mesh` envoy on `:8443`; ingress mTLS-originates to `www-mesh:3443` for the www UI. | Browser → HTTPS → ingress-mesh → www-mesh inbound → www → (iptables) → www-mesh outbound → server-mesh inbound → server. Ingress is north-south only; sidecar-mediated east-west keeps the per-language SDK plaintext-only — apps don't ship a TLS stack. | Mesh-aware LB (sidecar's outbound clusters) replaces the static `--target` dial when the prod mesh runtime takes over east-west; grpc-web bridge in front of www if browser-direct gRPC is ever wanted. |
| Wire schema | Single `.proto` shared between Rust and Next.js. | One language commits generated code; the other loads at runtime. | Real product schema; lint + breaking-change tooling layered on top. |
| LocalMesh CA bootstrap | `make certs` runs the localmesh Go CLI: `ca mint --force` + `ca install` + `mtls mint` + `build`. The CLI reads `project.toml` + every `localmesh/service_catalog/<plugin>/plugin.toml`, mints the LocalMesh CA into `localmesh/secrets/root_ca/` via vendored mkcert, mints per-service leaf certs into `localmesh/secrets/<container>/{trust.ca.crt,id.crt,id.key}` natively via `crypto/x509` (SPIFFE URI SAN + 7-day lifetime), regenerates the `.env` managed section (per-plugin identity tuple + per-service flags + mint-once secrets), renders each plugin's compose into `localmesh/bundled.compose.yaml`, and renders the mesh package's envoy YAMLs + iptables-init scripts into `localmesh/envoy/`. CA private key stays on the host. Host trust install via `mkcert -install` (wrapped by `localmesh ca install`). `make certs` always wipes and regenerates — no granular subcommand (out of scope for a take-home). The existing `make setup` target chains `certs` and is otherwise unchanged. | Single command bootstraps the entire mesh's identity surface from declarative TOML. New plugins drop a `plugin.toml`, re-run `make certs`, get certs + envoy routes + env-var bindings for free. mkcert subsumes `tools/step` for cross-platform trust-store install; native Go leaf minting gives SPIFFE URI SAN + lifetime control mkcert can't. | Replaced by a real secrets backend (cert-manager + cluster issuer, SPIRE Workload API for SVID delivery). Host trust install becomes real DNS + Let's Encrypt at the ingress. |
| Secrets injection | Repo-local secret store at `localmesh/secrets/` (gitignored). Cert tree under `localmesh/secrets/<container>/` (root CA at `localmesh/secrets/root_ca/`). Containers receive their cert bundle via docker compose secrets, which lands files at `/run/<container>/` with a fresh mode regardless of host-side uid (the bind-mount alternative trips envoy's drop-priv uid 101 against the host's 1000). Per-plugin identity tuple (`MODULE_NAME`, `OWNED_BY`) lands in `.env` (still at repo root for compose's default lookup; `TODO: needs_prod_decisions .env move under localmesh once config-loading tools improve`) via `localmesh build`, where compose interpolates it onto docker labels + `OTEL_RESOURCE_ATTRIBUTES`. The ingress envoy's `oauth2` filter reads its client secret + HMAC via file-based SDS at `/run/ingress-mesh/oauth2-{client-secret,hmac}.yaml`, written by `make oauth2-secrets` from the `LOCALMESH_OIDC_CLIENT_SECRET` env. File-based SDS sidesteps the envoy v1.31 static-resources duplicate-name trap. The Go CLI owns certs + `.env` + the merged compose + the envoy YAMLs. Compose labels (`katenary.v3/*`) still translate to K8s Secrets / ConfigMaps at chart render time. | One convention covers certs, IdP user lists, and dev secrets. `make clean` blows away the whole tree. Compose-label translation to K8s primitives unchanged at the team interface. | Same interface; secrets resolve from a real backend (Vault Agent / Secrets Manager / GSM) at runtime, certs from cert-manager / SPIRE. |
| Volume classes | Named local volume in dev; chart exposes a storage-class values field. | Dev doesn't need named classes; each env overlays the right one. | Named classes (`soc2_sensitive`, etc.) provisioned by the cluster. |
| Containers | Single-stage Dockerfiles for Rust and Next.js; everything else off-the-shelf. | Shortest build path; hardening costs build time without demo value. | Multi-stage, non-root, real healthchecks, signed images. |
| Image tags | Hardcoded placeholder. | The Helm generator needs a tag; no registry to push to locally. | CI-derived tag pushed with provenance + SBOM (Software Bill of Materials). |
| Compose-to-Helm labels | Standard label set from [`katenary-top-seven.md`](../engineering/rules/katenary-top-seven.md). Includes `katenary.v3/ports` on any port-less service that other services `depends_on` (cadvisor, node-exporter) — without it katenary can't generate the readiness init-container and chart-gen exits non-zero (upstream issue #182). Pre-commit regenerates the chart when compose or catalogue YAML changes. | Each label flips one specific compose→Helm translation. | May extend with org-specific custom labels. |
| README scope | `docker compose up`; runs in GitHub Codespaces per the take-home requirements. | Codespaces is the most predictable demo environment for reviewers. | Deployment runs via the org's standard CI/CD. |
| Live demo | CloudFlare Zero Trust tunnel + Access to author's K8s cluster. | Easy public URL with an auth gate; no warranty for other clusters. | Real ingress with cert-manager + org IdP (Identity Provider). |
| Logging platform contract | Stack-agnostic event envelope (one NDJSON event per stdout line: `timestamp` / `level` / `message` / `trace_id`+`span_id` when an OTel span is active / free-form attributes); three-tier cardinality contract (indexed labels for bounded dimensions, structured metadata for high-cardinality queryable fields, body for free text); the agent owns parse, enrich, validate, redact, sample, batch. Per-language emitter recipes (`tracing` + `tracing-subscriber` + `tracing-opentelemetry` for Rust, Pino + `@opentelemetry/api` for Node) and per-source agent extractor recipes (postgres jsonlog, nginx, grafana logfmt, etc.) keep teams omakase-conformant with minimal effort. Long form: [`docs/engineering/rules/logging-platform.md`](../engineering/rules/logging-platform.md). | Vendor-portable across Loki, Datadog, Splunk, Honeycomb, Elastic, GCP / AWS cloud logging — the envelope is the OTel logs data model and the cardinality contract is the same wherever you put it. Apps install one package and copy 5 lines; SREs extend the agent's job as far as they care to. | Per-source extractor verification for unfamiliar emitters; PII redaction processor wired in at the agent; per-environment retention + sampling tiers. |
| Logging | `logging/` catalogue module — Loki 3.x for storage, Vector 0.46 for ingest. Vector's `docker_logs` source tails the docker socket and exposes container labels on every event; a VRL `enrich` transform promotes `metrics.service_name` / `metrics.module_name` / `metrics.owned_by` to event fields. `parse_envelope` reads the app JSON contract out of `.message` (non-conforming sources fall through unchanged). A `route` transform splits by source: postgres jsonlog → `norm_postgres` (`error_severity` → contract `level` enum), grafana logfmt → `norm_grafana` (lvl → level + message extraction), everything else → `norm_default` (defaults `level=info`). A `validate` remap lowercases `level` and drops any out-of-enum value so Loki's label cardinality stays bounded. The `loki` sink ships indexed labels (service_name / module_name / owned_by / deployment_environment_name / level / container_id) and trace_id / span_id as structured metadata. otel-collector owns traces + metrics only — no filelog receiver, no loki exporter. K8s overlay swaps `docker_logs` → `kubernetes_logs` (VRL `.label.*` becomes `.kubernetes.pod_labels.*`); the rest carries over. Long form: [`docs/superpowers/specs/2026-05-17-logging-platform-contract-design.md`](../superpowers/specs/2026-05-17-logging-platform-contract-design.md). | Single agent owns the log path. Apps emit the envelope and nothing else; the agent supplies identity from container labels; severity is bounded at ingest so Loki labels stay bounded. Backend swap (Datadog / Splunk / managed Loki) means changing the sink, nothing else. | Managed Loki backend; per-source remaps for the no-op-default sources when SREs want severity filtering on them; PII redaction processor wired upstream of the sink; per-environment sampling tiers. |
| Structured-log contract | App containers (server, www) emit one JSON object per stdout line: `timestamp` (RFC 3339 UTC ms), `level` (lowercase enum: `trace`/`debug`/`info`/`warn`/`error`/`fatal`), `message`, `trace_id` + `span_id` (lowercase hex W3C, when an OTel span is active), plus free-form extras. Resource attributes (`service.name`, `module_name`, `owned_by`, `deployment.environment.name`) are **not** in the envelope — the Vector agent supplies them from container labels (next row). Rust: `tracing-subscriber::fmt::layer().json()` + `tracing-opentelemetry` bridge; root span is just `tracing::info_span!("app")`. The `ErrorLogLayer` Tower middleware logs non-OK gRPC `Status` returns at WARN/ERROR with `trace_id` and `span_id` pulled from the active OTel span (the JSON formatter doesn't auto-bridge those into event records). www: Pino with no `base:` block; `formatters.log` injects active OTel context. Per-language emitter recipes for other stacks (Go / Python / JVM / Ruby / .NET) live in the rules doc. | Apps stay dumb. Same JSON shape across languages. Conforming costs one package + 5 lines of init. | Per-language recipe verification for the four unverified rows when a service in any of those languages joins the catalogue. |
| Bonuses | All six items from `requirements.md` in scope. | OTel (OpenTelemetry)-everywhere carries logging and error handling; catalogue argument carries data design. | Per-bonus mapping in the spec. |
| Grafana API auth (dev) | Admin Basic Auth. Grafana service pins `GF_SECURITY_ADMIN_USER`/`PASSWORD` to `admin`/`admin`; catalogue services that call `/api/*` set `GRAFANA_ADMIN_USER`/`GRAFANA_ADMIN_PASSWORD` env and issue `Authorization: Basic`. No pre-seeded SA token — Grafana has no native pre-seed mechanism and the prior curl-bootstrap one-shot was retired. Browser users reach grafana through the ingress envoy with `requires_auth = false` on its `[[services]]` entry (the ingress virtual host skips `jwt_authn`); dashboards are browsed openly in dev. `TODO: needs_prod_decisions grafana auth via envoy in prod`. | YAGNI on a token until a consumer actually needs the production-shaped credential. Pinning the password in env is config-as-documentation: readers see the dev contract without knowing Grafana's defaults. Same shape `gcx` already uses to talk to `127.0.0.1:3001`. | Real IdP-issued credential (OIDC client-credentials or a real SA token minted by ops-side tooling). Pinned password disappears; consumers swap env block for the prod credential path. `TODO: needs_prod_decisions real IdP / SA token for Grafana API access`. |
| Service mesh — local model | One `mesh/` plugin runs an envoy fleet across three roles: an `ingress-mesh` gateway (north-south HTTPS on `:8443`, OIDC dance + JWT validation), one `<container>-mesh` sidecar per non-exempt workload (sharing its parent's netns via `network_mode: "service:<parent>"`, terminating inbound mTLS on the declared east-west port, originating outbound mTLS via transparent interception), and an `egress-mesh` gateway (internet-bound traffic, uniform allowlist v0). Per-workload iptables-init containers (`cap_add: ["NET_ADMIN"]` on the init only) lay down REDIRECT rules in the netns: outbound non-loopback SYNs → envoy :15001; inbound on the workload's declared port → envoy :15006. Envoy's outbound listener reads `SO_ORIGINAL_DST` and picks the destination cluster off the (peer, port) pair; the cluster list is derived from the workload's compose `depends_on` intersected with the plugin catalog (so `www`'s `depends_on: {server}` yields one edge to `server-mesh:50051`). Catch-all forwards to `egress-mesh`. Identity-of-record is a SPIFFE URI in every cert SAN; `forward_client_cert_details: SANITIZE_SET` (uri + subject) propagates peer identity to the inbound listener via XFCC. RBAC is allow-all-internal in dev (foundation tier; row 14 cert-as-policy deferred). Postgres native mTLS termination remains (carve-out — STARTTLS isn't transparent-proxyable). Observability containers, `logging`, dex, and postgres are mesh-exempt (`mesh.exempt: "true"` compose label; no certs minted, no sidecar). Long form: [`docs/superpowers/specs/2026-05-20-envoy-migration-design.md`](../superpowers/specs/2026-05-20-envoy-migration-design.md). | The mesh isn't a single plugin in name only — it's the SRE-prescribed default every workload participates in. Envoy + iptables + ORIGINAL_DST is the Istio / Linkerd pattern transplanted onto compose, so the dev shape matches the prod shape down to the syscall. Foundation rows from the envoy-omakase list land for free at boot: row 1 (OTel spans on every hop via `tracing.http`), row 2 (`:15090/stats/prometheus` per envoy), row 4 (`x-request-id` propagation), row 5 (XFCC peer identity), row 6 (JSON access logs with trace_id + peer SPIFFE URI), row 7 (TLS 1.3 east-west, no plaintext). Mesh-exempt stays a single greppable label that scales to default-deny policy generation later. | Sidecar runtime swaps to the prod mesh (Istio / Linkerd / Cilium eBPF) with native CRDs. SPIRE Workload API mints short-lived SVIDs. Default-deny `AuthorizationPolicy` enforced at the sidecar; mesh-exempt label becomes the CEL predicate the policy generator reads to write carve-outs. `TODO: needs_prod_decisions iptables init container vs eBPF (Cilium-style) for prod`. `TODO: needs_prod_decisions sidecar lifecycle binding to workload` (compose has no native pod-readiness equivalent). |
| Workload identity (SPIFFE shape) | Every workload holds one bidirectional cert (`server-`+`clientAuth` EKU) at `localmesh/secrets/<container>/`. CN = container name; SPIFFE URI SAN = `spiffe://<container>.${PROJECT_NAME}.${LOCAL_DOMAIN}`; DNS SANs cover the bare container name + the wildcard `<container>.${PROJECT_NAME}.${LOCAL_DOMAIN}`; IP SANs are intentionally over-permissive in dev. `plugin.toml` lists `[[services]]` entries (`container`, `port`, `scheme`, `expose_via_ingress`, `ingress`, `requires_auth`) per workload the plugin owns. The envoy sidecar's inbound listener carries peer identity onto a per-request header via `forward_client_cert_details: SANITIZE_SET` (XFCC with uri + subject). RBAC is allow-all-internal in dev; per-caller cert-as-policy lands when row 14 lands. Postgres still cert-CN-binds the client role natively because its sidecar carve-out keeps that path in-process. | One identity per workload, bidirectional, SPIFFE-faithful. Conventions baked into one CLI pass means a future generator change is one file. Envoy at L7 surfaces the peer SPIFFE URI to apps that want it (`x-forwarded-client-cert` header) without app-side TLS plumbing. Identity-of-record matches what SPIRE will issue in prod. | SVIDs minted on demand by SPIRE; short-lived (~1h); rotated automatically. Bidirectional EKU stays. IP SAN list drops to nothing (prod uses DNS-based identity exclusively). `TODO: needs_prod_decisions per-caller RBAC via cert-as-policy (row 14)`. |
| Trusted localdev TLS | LocalMesh CA installed into the host trust store via `mkcert -install`, wrapped by `localmesh ca install` (mkcert v1.4.4 vendored at `localmesh_src/tools/mkcert`, linux-amd64). CA material lives at `localmesh/secrets/root_ca/`. Browser hits `https://<container>.${PROJECT_NAME}.${LOCAL_DOMAIN}:8443` with no warning and the green padlock. `${LOCAL_DOMAIN}` defaults to `lvh.me` (magic DNS — every subdomain resolves to 127.0.0.1) so no `/etc/hosts` editing is needed; `local_domain` in `project.toml` overrides if a real local DNS is preferred. Wildcard cert SAN (`*.${PROJECT_NAME}.${LOCAL_DOMAIN}`) on the Caddy ingress so adding a new exposed UI doesn't need a re-mint. | The whole "is this cert trusted?" debate gets resolved at `make setup` time. `mkcert` is purpose-built for cross-platform trust-store registration (NSS / system store / Firefox); vendoring the binary removes the host-install prerequisite. lvh.me removes the last "sudo on first run" step. Wildcard cert in dev is convenient and explicitly TODO'd against prod. | Real internal DNS + cert-manager-issued IngressRoute certs at the cluster ingress. Per-host certs (no wildcards) in prod. |
| User identity at ingress | User identity is established at the `ingress-mesh` envoy via the `envoy.filters.http.oauth2` filter (OIDC client → dex) chained with `envoy.filters.http.jwt_authn` (validates the dex-signed JWT against dex's JWKS on every subsequent request). The oauth2 filter handles the dance (302 to dex, code-for-token exchange, encrypted session cookie); jwt_authn's `claim_to_headers` injects `x-user-id` (sub), `x-user-email`, `x-user-name` (preferred_username) onto the upstream request. The OTel ingress span gets `enduser.{id,email,preferred_username}` via an OTTL rule on otel-collector reading those same headers — the one site where PII lands on telemetry. Downstream services do **not** re-validate the JWT. www reads the `x-user-*` headers and forwards them as gRPC metadata to server. Trust comes from the mesh's mTLS channel (peer SPIFFE URI on XFCC), not from per-call cryptographic validation of the JWT. Downstream services receive that user identity but **do not echo PII onto their own OTel span attributes or indexed Loki labels**; Loki lines carry `jwt_kid` for forensic pivoting without indexing user identity. `Greeter.SayHello` persists `(issuer="forwarded", subject=user_id)` into `hello_messages` — the PII-at-ingress rule constrains *telemetry*, not *business data*; distinct lanes. Virtual hosts with `requires_auth = false` (`dex.*`, `envoy.*`, `grafana.*` in dev) skip `jwt_authn` entirely. `TODO: needs_prod_decisions phantom-token broker for cryptographic claim narrowing`. | Single SRE-owned enforcement point at the request edge — one place to apply hashing, redaction, consent gates, or query-time scrubbing. Mechanical control: no `setAttributes('enduser.*')` call sites exist downstream, so the rule isn't a code-review convention — it's a structural property of where the spans get the attributes from. GDPR data-minimization landed architecturally. Channel-trust on the gRPC metadata is the take-home shortcut; phantom-token broker is the production replacement. | Same ingress-only model. Vector / Tempo / Loki regex scrubbing tightened by SRE per data class. Dex swaps for a real IdP via `auth/` plugin replacement; JWKS cache TTL tuned to the prod IdP rotation cadence. Forwarded gRPC metadata replaced by a phantom-token broker that narrows claims per workload SPIFFE URI. `TODO: needs_prod_decisions claim-aware downstream (row 12)`. `TODO: needs_prod_decisions claim-filter at workload (row 15)`. |
| Plugin manifest | Every catalogue plugin ships a `plugin.toml` at its directory root. Today's schema scopes two sections: `[identity]` (carries the per-plugin halves of the `plugin-conventions.md` §1 identity tuple — `module_name`, `owned_by`) and `[[services]]` (one entry per workload the plugin runs — `container`, `port`, `scheme` (`grpc` / `http` / `https` / `tcp` — describes the *app's* loopback bind protocol, distinct from the on-wire mesh leg which is always mTLS+HTTP/2 between sidecars), optional `expose_via_ingress`, optional `ingress`, optional `requires_auth` (default `true`) — services that opt out of the ingress auth gate set this to `false`; the IdP itself (`dex` in the `auth/` plugin) is the canonical opt-out, and default-deny applies to every other exposed service). Mesh-exempt status is per-container compose-label state (`mesh.exempt: "true"`), not a TOML field. The same shape extends naturally to `[[services]]` in `project.toml` for app-tier workloads. The Go CLI consumes both files and emits certs + `.env` + the merged compose + the envoy YAMLs + iptables-init scripts. Every plugin also ships a `README.md` per the strict template in `docs/engineering/rules/plugin-conventions.md` §2; `plugin.toml` owns identity + service shape, `README.md` owns consumer wiring, with the overlap rule in §3. | Mirrors the `project.toml` pattern on the team-facing side. One manifest format per plugin lets tooling reason about the catalogue without parsing compose files. The `scheme` field makes ingress upstream-protocol decisions and the future `MESH_*_URL` env-var convention well-formed. The schema is intentionally small today; future additions (default-deny `[allows]`, declared `[[externals]]` egress allowlist, katenary chart selection, compliance flags) extend without breaking existing entries. | Same shape; rendered into helm chart values + observability attribution metadata by the katenary build. Eventual `[allows]` block translates to mesh-native `AuthorizationPolicy`. The `.env` interpolation bridge is replaced by a proper compose extension / build-step overlay generator. `TODO: needs_prod_decisions declared egress allowlist via plugin.toml [[externals]] (row 13)`. `TODO: needs_prod_decisions scope linter (row 10)`. |
| Sidecar runtime | `envoyproxy/envoy:v1.31-latest`; one bidirectional process per non-exempt workload (`<container>-mesh`) sharing the parent's netns via `network_mode: "service:<parent>"`. Transparent interception via a per-workload `<container>-mesh-init` container that runs `apk add iptables && /init.sh` once at boot under `cap_add: ["NET_ADMIN"]` (envoy itself runs unprivileged). The init script REDIRECTs outbound non-loopback SYNs to envoy :15001 and inbound traffic on the workload's declared port to envoy :15006. Outbound listener uses `use_original_dst: true`; filter-chains key on `destination_port` to pick the right `<target>-mesh` cluster. Inbound listener carries XFCC (`forward_client_cert_details: SANITIZE_SET`, uri + subject) and forwards plaintext to the app's loopback port. mTLS via the LocalMesh CA at `localmesh/secrets/<parent>/`; certs delivered via docker compose `secrets:` (lands files at `/run/<container>/` with a fresh mode, sidesteps the envoy-image uid 101 vs host uid 1000 mismatch the bind-mount alternative hits). Foundation-tier omakase rows that land for free at boot: row 1 (`tracing.http` → otel-collector:4317), row 2 (`:15090/stats/prometheus` per envoy, scraped via `docker_sd_configs`), row 4 (request_id propagation), row 5 (XFCC peer identity), row 6 (JSON access logs with trace_id + request_id + peer SPIFFE URI + jwt_kid), row 7 (TLS 1.3 east-west only). Deferred rows: 3 (outlier detection — needs CLI caller↔callee join), 8–9 (retry / timeout schemas), 10 (scope linter), 11 (header stripping on egress), 12 (claim-aware downstream), 14 (cert-as-policy), 15 (claim-filter at workload). | One envoy process per workload covers both directions. Envoy at L7 surfaces peer SPIFFE URI to apps via XFCC, gives native Prom-text stats, and lets the same plugin own ingress + sidecar + egress without three different runtimes. The init+cap_add pattern works on Linux Docker today and matches Istio/Linkerd; eBPF lands when prod picks it. Mesh-exempt label + dex-out-of-mesh keeps the IdP+observability+postgres carve-outs greppable. | Real mesh runtime (Istio / Linkerd / Cilium eBPF) replaces the compose-side envoy with a native sidecar + control plane; `AuthorizationPolicy` replaces the dev's allow-all-internal RBAC; pod-readiness probes replace `depends_on: condition: service_completed_successfully`. `TODO: needs_prod_decisions iptables init container vs eBPF (Cilium-style) for prod`. `TODO: needs_prod_decisions multi-arch iptables-init image` (alpine:3.20 covers linux-amd64; arm64 needs verification). `TODO: needs_prod_decisions outlier detection (row 3)`. `TODO: needs_prod_decisions retry / timeout schemas (rows 8–9)`. `TODO: needs_prod_decisions header stripping on egress (row 11)`. |
| Plugin templating + CLI | Go module at `localmesh_src/` (workspace via `go.work` at repo root so `go run ./localmesh_src/cmd/localmesh ...` works from project root) ships five v0 verbs: `build` (renders `.env`'s managed section via `internal/envwriter`, merges plugin compose files into `localmesh/bundled.compose.yaml`, and renders the mesh package's envoy YAMLs + iptables-init scripts into `localmesh/envoy/`), `ca mint --force` (LocalMesh CA via mkcert subprocess), `ca install` / `ca uninstall` (host trust-store via `mkcert -install`), `mtls mint` (per-service leaf certs natively via `crypto/x509` with SPIFFE URI SAN + 7-day lifetime). Templates use Go's `text/template` + sprig with `Option("missingkey=error")`; plain `.yml` plugins pass through unchanged. Hybrid cert path: mkcert handles CA + trust-store install via subprocess (vendored binary at `localmesh_src/tools/mkcert`, linux-amd64), native crypto/x509 mints leaves (mkcert can't write SPIFFE URI SAN or sub-mkcert-default lifetimes). The `internal/mesh/` package owns the envoy-render side: a `catalog` of `{container → Service}` built from project + plugins, `edges` derivation walking each non-exempt workload's compose `depends_on` and intersecting with the catalog (dropping mesh-exempt and unresolved targets, failing the build on port collisions), per-role template renders (ingress / per-workload sidecar / egress) into `localmesh/envoy/`, and per-workload iptables-init scripts. Cert delivery to envoy containers goes through docker compose `secrets:` to sidestep the uid mismatch between envoy's drop-priv uid 101 and host bind-mount uid 1000. The ingress envoy's oauth2 filter reads its client secret + HMAC via file-based SDS at `/run/ingress-mesh/oauth2-{client-secret,hmac}.yaml`, written by `make oauth2-secrets` from `.env`. Fixture plugins under `localmesh_src/testdata/sample_catalog/` drive golden tests. The legacy `tools/step` binary is deleted (mkcert subsumes). | Sprig templates collapse plugin boilerplate (identity stamps, dashboard contributions, sidecar blocks) where repetition is real; opt-in per plugin avoids forcing a port. `depends_on`-driven edge derivation means apps declare nothing new — the compose edges already authored for ordering also describe the mesh's per-cluster filter chains. mkcert wins for cross-platform trust-store registration; native Go leaf minting wins for SPIFFE URI SAN + lifetime control mkcert can't express. Docker secrets for certs + SDS file mounts for oauth2 secrets keeps every sensitive blob owned by the CLI, not handed to a bind-mount. | katenary consumed as a Go dep instead of a subprocess; mkcert imported via go module rather than vendored binary; `kubernetes_sd_configs` replaces `docker_sd_configs` in prod (cross-link to spec); SPIRE Workload API mints SVIDs at ~1h instead of 7-day leaves; `validate` verb when a Go schema validator is picked. `TODO: needs_prod_decisions MESH_*_{HOST,PORT,URL} auto-emission` (CLI emits per-consumer env vars once the app-side adoption pattern settles). |
| Prometheus scrape discovery | `docker_sd_configs` against the host docker daemon (RO socket mount on the otel-collector). Emitter containers opt in via `prometheus.io/scrape=true`; port + path come from `prometheus.io/{port,path}` container labels. Identity tuple (`metrics.service_name`, `metrics.module_name`, `metrics.owned_by`) propagates from container labels into series labels via `relabel_configs`. The relabel writes `target_label: job` (not `service_name`) — the OTel prometheus receiver translates Prom `job` → OTLP `service.name`, which becomes the `service_name` series label after `resource_to_telemetry_conversion`. That's the same path the old static jobs took (`job_name: envoy` → `service_name=envoy`) and matches Pattern B's OTLP-pushed metrics, so the two paths converge on one cross-pattern identity label. `service_name` carries module identity, not container identity — cadvisor and node-exporter both stamp `metrics.service_name=observability` because that's their module, and per-container differentiation lives on the `job` and `name` labels. Cadvisor's three carry-forward `metric_relabel_configs` run at the job level: project filter with an empty-alternative regex (`${PROJECT_NAME}` or empty) so it no-ops on non-cadvisor series in the shared job, plus the two-step `service` label fallback (copy `container_label_metrics_service_name`, fallback to container `name` via `;(.+)`). otel-collector runs as `user: "0:0"` for docker-socket-group access (cadvisor precedent for privileged socket); `TODO: needs_prod_decisions docker socket scope`. | Adding a Pattern-A emitter is three labels on a compose service. Zero edits to observability's collector config. The omakase contract shifts from "ask SRE to add a scrape job" to "label your container." Same label convention works in prod via `kubernetes_sd_configs` reading pod labels. | `kubernetes_sd_configs` replaces `docker_sd_configs`; docker socket privilege concern goes away. AuthorizationPolicy-style label filtering on the SD side. Pod-label parity carries the `prometheus.io/*` + `metrics.*` convention forward. localmesh CLI follow-up will emit the labels declaratively from `plugin.toml` `[[exports.metrics]]` entries. `TODO: needs_prod_decisions localmesh CLI emits prometheus.io/* labels from plugin.toml [[exports.metrics]]`. |

### Process notes (not architecture)

- **Tests:** Rust unit/integration in `server/`; Next.js tests in `www/`. High-value tests only — PoC (Proof of Concept) scope.
- **Commit hygiene:** test changes and code changes never share a commit; code commits require a green tree.

## Open

V0 questions still in flux. Production-scope questions live in [`PRODUCTION_DISCUSSIONS.md`](./PRODUCTION_DISCUSSIONS.md).

- **mTLS demonstration depth.** Minimum: server logs successful connection at startup. Reviewer-visible: on-demand gRPC method + UI button.
- **Collector → TSDB mTLS on the dev path.** Plain HTTP for now; uniform with the Rust→TSDB hop is possible at small cost.
- **Outbound client metrics.** Apps now emit OTel-semconv server metrics (`rpc.server.duration` from the Rust tower middleware; `http.server.request.duration` from `@opentelemetry/instrumentation-http` in www) — those land the RED rows on the imported APM dashboard. Outbound client metrics (`http.client.duration`, `db.client.duration`, `rpc.client.duration`) are still partial: Node's auto-instrumentation emits HTTP-client metrics by default; the Rust client paths (reqwest → Grafana, sqlx → postgres, tonic-client from www-as-grpc-client) need explicit instrumentation. Outbound panels on the dashboard stay sparse until that lands.
- **Panel types beyond Timeseries.** Stat and Table variants stubbed in `metrics.proto` — handler returns `UNIMPLEMENTED`. Wired when a reviewer use-case appears; panel JSON differs only in `type` + a couple of option fields per type, so it's mostly mechanical.
- **Identity source consolidation.** `plugin.toml` (and `project.toml` for app-tier services) is now the source of truth for the per-plugin halves of the identity tuple, with the binding convention canonical in [`plugin-conventions.md`](../engineering/rules/plugin-conventions.md) §1. `localmesh build` (`internal/envwriter`) reads them and emits per-plugin UPCASE_SNAKECASE entries into `.env`'s managed section; compose interpolates them onto docker labels + `OTEL_RESOURCE_ATTRIBUTES`. Working bridge; the proper replacement (build-step compose overlay generator, or a real compose extension once compose grows one) is `TODO: needs_prod_decisions plugin.toml → compose interpolation` in `TODO.md`.
- **Convention linter.** Plugin conventions (identity tuple + `README.md` per plugin, per [`plugin-conventions.md`](../engineering/rules/plugin-conventions.md)) ship as documentation now. A v0+1 linter should check: every catalog plugin's services carry the full identity tuple matching the directory slug; every app-tier service carries `module_name=app`; every plugin directory with a `docker-compose.yml` and a consumer surface has a `README.md`; each `README.md` has the eight section headings in order. See [`2026-05-18-localmesh-namespacing-and-plugin-exports-design.md`](../superpowers/specs/2026-05-18-localmesh-namespacing-and-plugin-exports-design.md) §3.4 + §4.6.
- **Default-dashboard reconciliation.** Picker cleanup across the default dashboards (`database/postgres.json`, `observability/apm.json`, `observability/cluster-health.json`, and the future ingress dashboard once it's rewritten against envoy metrics) — drop the `environment` picker, standardise identity-axis pickers on `namespace` + `service`/`container_name`. Deferred from the conventions implementation because parallel dashboard work would intersect; intent + constraints captured in `docs/TODO.md`'s Dashboards section.

## Deviations from `requirements.md`

- **Agent → Server arrow is layered.** Off-the-shelf collector receives; TSDB stores; Rust server is the application API in front. *"The server"* in the requirements' sense is the layered tier.
- **Server in Rust** — satisfies the literal requirement.
- **Agent in Python** (Tasks section) — superseded by Core Requirements' "any language." Off-the-shelf collector instead, packaged as a catalogue module.
