# localmesh_src/tools/

`mkcert` — vendored binary, v1.4.4 linux-amd64. Wraps cross-platform host
trust-store install for the root CA. See spec §2.7.

TODO: needs_prod_decisions go mod import of mkcert replaces vendored binary
TODO: needs_prod_decisions detect arch beyond linux-amd64
