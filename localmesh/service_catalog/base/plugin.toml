# plugins must precede [identity]: a bare key after a table header is
# parsed into that table, so `plugins` here would become identity.plugins.
plugins = ["auth", "security"]

[identity]
module_name = "base"
owned_by    = "sre@example.com"
