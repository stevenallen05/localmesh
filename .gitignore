/www/node_modules
/www/.next
/server/target
/agent/agent

# TODO find a good presentation for env files 
# .env
# .env.*
# !.env.example

scratch.*

/www/*.tsbuildinfo

# Produces a lot of agentic noise
docs/superpowers
.superpowers/

.worktrees/
.project-ai

# LocalMesh dev artifacts (CA + per-plugin certs + per-plugin env + user list)
.localmesh/
.env.localmesh
