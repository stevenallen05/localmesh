/www/node_modules
/www/.next
/server/target
/agent/agent

# .env is dev-machine state — regenerated by `make certs` from project.toml +
# service_catalog/*/plugin.toml. Fresh clones run `make setup` to populate.
.env
.env.*
!.env.example

scratch.*

/www/*.tsbuildinfo

# Produces a lot of agentic noise
docs/superpowers
.superpowers/

.worktrees/
.project-ai
/.claude/

# LocalMesh dev artifacts (CA + per-service certs + dev user list)
.secrets/

# Caddyfile generated by `make certs` from plugin.toml + project.toml
service_catalog/caddy/Caddyfile.generated

# Python bytecode caches from scripts/ tests
__pycache__/
*.pyc
.pytest_cache/
service_catalog/auth/dex.yaml.generated
