services:
  agent:
    build: ./agent
    environment:
      AGENT_LOG_FORMAT: text
      # ${VAR:-} = pass through from shell, empty-string fallback. Used by §7.5
      # and §7.6. Bare-key form panicked katenary rc6; agent treats "" as default.
      AGENT_ID: ${AGENT_ID:-}
    # v0: only the agent service. server, db, www land in subsequent specs.
    # Kept minimal so `katenary` has the shape it needs without extra hints.
# This is a test of the commit hook