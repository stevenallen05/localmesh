# This file is autogenerated by katenary
#
# DO NOT EDIT IT BY HAND UNLESS YOU KNOW WHAT YOU ARE DOING
#
# If you want to change the content of this file, you should edit the
# compose file and run katenary again.
# If you need to override some values, you can do it in a override file
# and use the -f flag to specify it when running the helm command.



# cadvisor configuration
cadvisor:
  # key: cadvisor.repository
  repository:
    # key: cadvisor.repository.image
    image: gcr.io/cadvisor/cadvisor
    # key: cadvisor.repository.tag
    tag: v0.52.1
  # key: cadvisor.replicas
  replicas: 1
  # key: cadvisor.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: cadvisor.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: cadvisor.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: cadvisor.serviceAccount
  serviceAccount: ""

# grafana configuration
grafana:
  # key: grafana.repository
  repository:
    # key: grafana.repository.image
    image: grafana/grafana
    # key: grafana.repository.tag
    tag: 11.4.0
  # key: grafana.persistence
  persistence:
    # key: grafana.persistence.grafana_data
    grafana_data:
      # Storage class to use for PVCs
      # storageClass: "-" means use default
      # storageClass: "" means do not specify
      # storageClass: "foo" means use that storageClass
      # key: grafana.persistence.grafana_data.storageClass
      storageClass: '-'
      # key: grafana.persistence.grafana_data.size
      size: 1Gi
      # key: grafana.persistence.grafana_data.accessMode
      accessMode:
        - ReadWriteOnce
      # key: grafana.persistence.grafana_data.enabled
      enabled: true
  # key: grafana.replicas
  replicas: 1
  # key: grafana.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: grafana.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: grafana.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: grafana.serviceAccount
  serviceAccount: ""

# node_exporter configuration
node_exporter:
  # key: node_exporter.repository
  repository:
    # key: node_exporter.repository.image
    image: prom/node-exporter
    # key: node_exporter.repository.tag
    tag: v1.8.2
  # key: node_exporter.replicas
  replicas: 1
  # key: node_exporter.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: node_exporter.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: node_exporter.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: node_exporter.serviceAccount
  serviceAccount: ""

# otel_collector configuration
otel_collector:
  # key: otel_collector.repository
  repository:
    # key: otel_collector.repository.image
    image: otel/opentelemetry-collector-contrib
    # key: otel_collector.repository.tag
    tag: 0.152.0
  # key: otel_collector.replicas
  replicas: 1
  # key: otel_collector.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: otel_collector.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: otel_collector.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: otel_collector.serviceAccount
  serviceAccount: ""

# imagePullSecrets allows you to specify a name of an image pull secret.
# You must provide a list of object with the name field set to the name of the
# e.g.
# pullSecrets:
# - name: regcred
# You are, for now, responsible for creating the secret.
pullSecrets: []

# redis configuration
redis:
  # key: redis.repository
  repository:
    # key: redis.repository.image
    image: redis
    # key: redis.repository.tag
    tag: 7.4-alpine
  # key: redis.persistence
  persistence:
    # key: redis.persistence.redis_data
    redis_data:
      # Storage class to use for PVCs
      # storageClass: "-" means use default
      # storageClass: "" means do not specify
      # storageClass: "foo" means use that storageClass
      # key: redis.persistence.redis_data.storageClass
      storageClass: '-'
      # key: redis.persistence.redis_data.size
      size: 1Gi
      # key: redis.persistence.redis_data.accessMode
      accessMode:
        - ReadWriteOnce
      # key: redis.persistence.redis_data.enabled
      enabled: true
  # key: redis.replicas
  replicas: 1
  # key: redis.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: redis.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: redis.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: redis.serviceAccount
  serviceAccount: ""

# server configuration
server:
  # key: server.repository
  repository:
    # key: server.repository.image
    image: metrics-stack/server
    # This is the version of the main application.
    # Leave it to blank to use the Chart "AppVersion" value.
    # key: server.repository.tag
    tag: ""

  # key: server.replicas
  replicas: 1
  # key: server.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: server.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: server.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: server.serviceAccount
  serviceAccount: ""

# tempo configuration
tempo:
  # key: tempo.repository
  repository:
    # key: tempo.repository.image
    image: grafana/tempo
    # key: tempo.repository.tag
    tag: 2.10.5
  # key: tempo.persistence
  persistence:
    # key: tempo.persistence.tempo_data
    tempo_data:
      # Storage class to use for PVCs
      # storageClass: "-" means use default
      # storageClass: "" means do not specify
      # storageClass: "foo" means use that storageClass
      # key: tempo.persistence.tempo_data.storageClass
      storageClass: '-'
      # key: tempo.persistence.tempo_data.size
      size: 1Gi
      # key: tempo.persistence.tempo_data.accessMode
      accessMode:
        - ReadWriteOnce
      # key: tempo.persistence.tempo_data.enabled
      enabled: true
  # key: tempo.replicas
  replicas: 1
  # key: tempo.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: tempo.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: tempo.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: tempo.serviceAccount
  serviceAccount: ""

# victoriametrics configuration
victoriametrics:
  # key: victoriametrics.repository
  repository:
    # key: victoriametrics.repository.image
    image: victoriametrics/victoria-metrics
    # key: victoriametrics.repository.tag
    tag: v1.106.0
  # key: victoriametrics.persistence
  persistence:
    # key: victoriametrics.persistence.vm_data
    vm_data:
      # Storage class to use for PVCs
      # storageClass: "-" means use default
      # storageClass: "" means do not specify
      # storageClass: "foo" means use that storageClass
      # key: victoriametrics.persistence.vm_data.storageClass
      storageClass: '-'
      # key: victoriametrics.persistence.vm_data.size
      size: 1Gi
      # key: victoriametrics.persistence.vm_data.accessMode
      accessMode:
        - ReadWriteOnce
      # key: victoriametrics.persistence.vm_data.enabled
      enabled: true
  # key: victoriametrics.replicas
  replicas: 1
  # key: victoriametrics.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: victoriametrics.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: victoriametrics.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: victoriametrics.serviceAccount
  serviceAccount: ""

# www configuration
www:
  # key: www.repository
  repository:
    # key: www.repository.image
    image: metrics-stack/www
    # key: www.repository.tag
    tag: 0.1.0
  # Default value for ingress.class annotation
  # class: "-"
  # If the value is "-", controller will not set ingressClassName
  # If the value is "", Ingress will be set to an empty string, so
  # controller will use the default value for ingressClass
  # If the value is specified, controller will set the named class e.g. "nginx"
  # key: www.ingress
  ingress:
    # key: www.ingress.annotations
    annotations: {}
    # key: www.ingress.host
    host: www.tld
    # key: www.ingress.path
    path: /
    # key: www.ingress.class
    class: '-'
    # key: www.ingress.enabled
    enabled: false
    # Ingress TLS configuration
    # If enabled, a secret containing the certificate and the key should be
    # created by the ingress controller. If the name if emtpy, so the secret
    # name is generated. You can specify the secret name to use your own secret.
    # key: www.ingress.tls
    tls:
      # key: www.ingress.tls.enabled
      enabled: true
      # key: www.ingress.tls.secretName
      secretName: ""
  # key: www.replicas
  replicas: 1
  # key: www.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: www.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: www.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: www.serviceAccount
  serviceAccount: ""

# vim: ft=yaml
